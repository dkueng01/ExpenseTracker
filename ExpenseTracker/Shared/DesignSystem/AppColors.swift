import SwiftUI

enum AppColors {
    static let screenBackground = Color(.systemGroupedBackground)

    static let cardBackground = Color(.secondarySystemGroupedBackground)
    static let elevatedBackground = Color(.secondarySystemGroupedBackground)
    static let inputBackground = Color(.secondarySystemBackground)

    static let cardBorder = Color(.separator).opacity(0.18)
    static let controlBorder = Color(.separator).opacity(0.24)

    static let primaryTint = Color.blue
    static let disabledButton = Color.gray.opacity(0.4)

    static let primaryText = Color.primary
    static let secondaryText = Color.secondary
}
