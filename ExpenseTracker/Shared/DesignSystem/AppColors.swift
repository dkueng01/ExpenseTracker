import SwiftUI

enum AppColors {
    static let screenBackground = Color(.systemGroupedBackground)

    static let cardBackground = Color(.systemBackground)
    static let elevatedBackground = Color(.systemBackground)
    static let inputBackground = Color(.systemBackground)

    static let cardBorder = Color(.separator).opacity(0.10)
    static let controlBorder = Color(.separator).opacity(0.18)

    static let primaryTint = Color.blue
    static let disabledButton = Color.gray.opacity(0.4)

    static let primaryText = Color.primary
    static let secondaryText = Color.secondary
}
